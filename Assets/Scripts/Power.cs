using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Power : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        print(Mathf.Pow(2, 24));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
